<%= form_for(@brew_type) do |f| %>
  <% if @brew_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@brew_type.errors.count, "error") %> prohibited this brew_type from being saved:</h2>

      <ul>
      <% @brew_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
